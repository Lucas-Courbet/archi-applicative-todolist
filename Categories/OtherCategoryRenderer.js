class OtherCategoryRenderer extends TaskRenderer {
    /**
     * Implementation for rendering tasks in Category Other
     */
    render() {
        console.log(`Rendering task in Category Other: ${this.task.text}`);
    }
}