class WorkCategoryRenderer extends TaskRenderer {
    /**
     * Implementation for rendering tasks in Category Work
     */
    render() {
        console.log(`Rendering task in Category Work: ${this.task.text}`);
    }
}